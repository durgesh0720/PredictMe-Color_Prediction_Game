# Testing Environment Configuration
# This file is for running tests - secure but allows HTTP for testing

# Django Configuration
SECRET_KEY=wkk568b3d3=()o%jz4#d%j4vey%-4=%%s1chr7q-xv-#gv*)1j
DEBUG=False
ALLOWED_HOSTS=127.0.0.1,localhost,testserver

# Database Configuration (SQLite for testing)
DB_ENGINE=django.db.backends.sqlite3
DB_NAME=:memory:
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=

# Redis Configuration (Local)
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Security Settings - TESTING (Secure but allows HTTP)
CSRF_TRUSTED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000,http://testserver
SESSION_COOKIE_SECURE=False
SESSION_COOKIE_AGE=3600
ADMIN_SESSION_TIMEOUT=1800

# HTTPS Security Settings - DISABLED FOR TESTING
SECURE_SSL_REDIRECT=False
SECURE_HSTS_SECONDS=0
SECURE_HSTS_INCLUDE_SUBDOMAINS=False
SECURE_HSTS_PRELOAD=False
SECURE_CONTENT_TYPE_NOSNIFF=True
SECURE_BROWSER_XSS_FILTER=True
SECURE_REFERRER_POLICY=strict-origin-when-cross-origin
X_FRAME_OPTIONS=DENY
CSRF_COOKIE_SECURE=False
CSRF_COOKIE_HTTPONLY=True

# Email Configuration (Testing - Console Backend)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=
EMAIL_PORT=
EMAIL_USE_TLS=False
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=test@example.com

# Logging Configuration
LOG_LEVEL=INFO
LOG_FILE_PATH=logs/django.log

# API Security (Relaxed for testing)
API_RATE_LIMIT_PER_MINUTE=1000
API_RATE_LIMIT_PER_HOUR=10000

# Game Configuration
ROUND_DURATION=50
BETTING_DURATION=40
RESULT_DISPLAY_DURATION=10

# Wallet Configuration
MIN_BET_AMOUNT=1
MAX_BET_AMOUNT=10000
MIN_WALLET_BALANCE=0
DEFAULT_BALANCE=0

# Payment Gateway Configuration (Test Mode)
RAZORPAY_KEY_ID=rzp_test_your_test_key
RAZORPAY_KEY_SECRET=your_test_secret
RAZORPAY_WEBHOOK_SECRET=test_webhook_secret

# Payment Limits (Testing)
MIN_DEPOSIT_AMOUNT=1
MAX_DEPOSIT_AMOUNT=1000
MIN_WITHDRAWAL_AMOUNT=1
MAX_WITHDRAWAL_AMOUNT=500

# Admin Configuration
ADMIN_PANEL_RATE_LIMIT=10000
ADMIN_SESSION_TIMEOUT=7200

# WebSocket Configuration
WS_ALLOWED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000,http://testserver

# Production Settings (Disabled for testing)
STATIC_ROOT=staticfiles/
MEDIA_ROOT=media/
USE_WHITENOISE=False

# Additional Payment Security (Relaxed for testing)
PAYMENT_VERIFICATION_REQUIRED=False
FRAUD_DETECTION_ENABLED=False
MAX_DAILY_DEPOSIT_LIMIT=10000
MAX_DAILY_WITHDRAWAL_LIMIT=5000

# Responsible Gambling Configuration (Testing - Very high limits)
RG_DAILY_LOSS_LIMIT=100000
RG_DAILY_BET_LIMIT=200000
RG_SESSION_LOSS_LIMIT=50000
RG_SESSION_TIME_LIMIT=28800
RG_MAX_BET_AMOUNT=20000
RG_MIN_BET_AMOUNT=1
RG_COOLING_OFF_PERIOD=60
